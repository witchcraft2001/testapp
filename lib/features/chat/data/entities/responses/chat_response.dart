class ChatResponse {
  int id;
  String avatar;
  String name;
  String lastMessage;
  DateTime lastMessageTime;

  ChatResponse(this.id, this.avatar, this.name, this.lastMessage, this.lastMessageTime);
}
