// Project imports:
import 'form_field_message.dart';

class DateFormFieldMessage extends FormFieldMessage {
  late final String description;

  DateFormFieldMessage(super.id, super.title, this.description);
}
